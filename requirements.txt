requests>=2.31
PyYAML>=6.0

# Optional: Claude-written front-page briefing via the API provider.
# Not needed if you use provider: claude-cli (Claude Code + subscription).
anthropic>=0.40

# Optional: e-ink PNG rendering (also run: playwright install chromium)
# playwright>=1.40

# Optional: Gmail + Google Calendar sections (uncomment to enable)
# google-api-python-client>=2.100
# google-auth-oauthlib>=1.1
